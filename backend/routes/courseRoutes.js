const express = require('express');
const router = express.Router();

// Import controllers
const courseController = require('../controllers/courseController');

// Routes will be added here
// GET /api/courses
// GET /api/courses/:id
// GET /api/courses/:id/careers

module.exports = router;
